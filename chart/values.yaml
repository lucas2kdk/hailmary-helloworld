replicaCount: 1

image:
  repository: hello-world
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: false
  className: ""
  host: hello.local
  path: /
  pathType: Prefix

resources: {}
